# Working With JSON

JSON for this session lives in the teach-from **[Scraping the Web](WebScraping.md)** — extract with Colly, then `encoding/json` marshal to `output.json`.

Update your bookmarks to `Lessons/WebScraping.md`.
